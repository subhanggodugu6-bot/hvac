# Vercel UI. Set the Render API host here, then redeploy so NEXT_PUBLIC_* is
# baked into the client bundle. Use the exact host Render assigned.

HVAC_API_ORIGIN=https://hvac-api-elin.onrender.com
NEXT_PUBLIC_API_URL=https://hvac-api-elin.onrender.com/api
